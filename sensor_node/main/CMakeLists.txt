idf_component_register(
    SRCS
        "app_node_main.c"
    INCLUDE_DIRS
        "."
    REQUIRES
        nvs_flash
        esp_event
        esp_netif
        openthread
        app_sensor
        app_coap_client
        app_sleep
        driver
        esp_timer
        vfs
        esp_https_ota
        esp_http_client
        app_update
        mbedtls
)
